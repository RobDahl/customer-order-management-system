namespace Coms.Domain.Notes
{
    /// <summary>Stored as text; names must match CK_Notes_EntityType.</summary>
    public enum NoteEntityType
    {
        Customer,
        Order,
        Invoice
    }
}
